apiVersion: v1
kind: Pod
metadata:
  name: get-image-rpi3
spec:
  containers:
  - name: get-image-rpi3
    image: wakeup706/rpi-camera-mqtt:info 
    command: ["/bin/bash","-c","python image-mqtt.py"]
    env:
    - name: TOPIC
      value: "image"
    - name: BROKER
      value: "140.114.79.70"
    securityContext:
      privileged: true
    volumeMounts:
    - mountPath: /dev/mem
      name: mem
  restartPolicy: Never
  volumes:
  - name: mem
    hostPath:
      path: /dev/mem
  nodeSelector:
    device: rpi3
